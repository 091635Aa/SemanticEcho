\section{引言}

\subsection{当前范式的隐性缺陷：概率暴政}

自回归语言模型通过在每一步预测下一个Token的条件概率分布来生成文本~\cite{vaswani2017attention,radford2018improving,brown2020language}。
在推理阶段，模型从候选词汇表$\mathcal{V}$中选取一个Token $x_t^*$作为输出，而其余$|\mathcal{V}|-1$个候选Token的隐藏状态被完全丢弃。
本文将这一机制性浪费称为\textbf{概率暴政}（Probabilistic Tyranny）。

从信息论角度，每一步解码计算了完整的条件概率分布，但仅利用了一个采样点的信息。
对于一个词汇量$|\mathcal{V}|=151{,}936$、隐藏维度$d_{\text{model}}=896$的模型，每步丢弃约544MB信息，$T=200$步总共丢弃超过100GB。
这些信息并非无意义的噪声它们是模型对"可能性空间"的全景式理解。

更关键的是，当前范式在每一步系统性地忽略了未被选中Token的语义信息。
这本质上是\textbf{推理信息流路径}的设计缺陷：候选Token的隐藏状态在计算完成后仅服务于softmax归一化，一旦采样完成便被物理清除。
语义回响的核心创新在于\textbf{修改推理阶段的信息流路径}，将这些逻辑上的"孤儿节点"重新接入计算图。

尽管采样策略（如top-$p$~\cite{holtzman2020curious}）缓解了多样性困境，但它们仅在\textbf{输出概率空间}操作。
\emph{语义回响在表示层操作}，与基于采样的多样性增强方法\textbf{是互补而非竞争关系}。

\subsection{人类交流中的潜台词与回响}

在人类对话中，信息的传递不仅依赖于已说出的词，还依赖于未被说出的"潜台词"。
本文的核心主张：\textbf{情感不是由被说出的词决定的，而是由那些没有被说出但被激活的词决定的。}
那些被激活却未被选中的词尤其是情感色彩强烈的词构成了模型的"情感潜台词"。
语义回响正是要捕获并利用这一隐式调性。

\subsection{与现有方法的本质区别}

与CTRL~\cite{keskar2019ctrl}、PPLM~\cite{dathathri2020plug}等方法不同，语义回响：
（1）不修改模型权重；（2）不重新训练；（3）不改动核心注意力架构；（4）即插即用；（5）核心是信息流路径创新。

它本质上是一种\textbf{推理策略}，而非训练方法或模型架构。

\subsection{本文贡献}

（1）新范式：首个系统性地回收"语义废料"的研究；
（2）新机制：情感词库筛选和思考阶段分离注入；
（3）新发现：$\lambda$的U型效应、情感筛选的缓解重复效应、三种保留策略优劣排序；
（4）开源实践：CC BY-NC 4.0许可证发布。
